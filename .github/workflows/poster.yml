name: Medium Auto Poster

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: pip install selenium feedparser webdriver-manager selenium-stealth

      - name: Run Python Script
        id: script
        env:
          MEDIUM_SID_COOKIE: ${{ secrets.MEDIUM_SID_COOKIE }}
          MEDIUM_UID_COOKIE: ${{ secrets.MEDIUM_UID_COOKIE }}
        run: python main.py
        
      # --- هذا هو القسم الجديد والمهم ---
      - name: Commit and Push Log File
        run: |
          # تحقق أولاً مما إذا كان ملف السجل قد تم إنشاؤه
          if [ -f posted_links.txt ]; then
            git config --global user.name 'GitHub Actions Bot'
            git config --global user.email 'actions-bot@github.com'
            git add posted_links.txt
            # قم بالـ commit فقط إذا كانت هناك تغييرات
            git diff --staged --quiet || git commit -m "Update posted links log"
            git push
          else
            echo "Log file not found, nothing to commit."
          fi
